<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.web.domain.community.mapper.FreeBoardMapper">

	<select id="freeBoardList" resultType="com.sist.web.domain.community.vo.FreeBoardVO" parameterType="hashmap">
		SELECT b.no,m.member_id,b.hit,b.subject,NVL(m.name, b.name) AS name,TO_CHAR(b.regdate,'yyyy-mm-dd') AS dbday,
		       (SELECT COUNT(*) FROM FREE_COMMENT WHERE BOARD_NO=b.no) as cCount
		FROM free_board b
		LEFT JOIN member m ON b.member_id=m.member_id
		<if test="fd!=null and fd!=''">
			WHERE b.subject LIKE '%'||#{fd}||'%'
		</if>
		ORDER BY no DESC
		OFFSET #{start} ROWS FETCH NEXT 20 ROWS ONLY
	</select>
	
	<select id="freeBoardCount" resultType="int" parameterType="hashmap">
		SELECT COUNT(*)
		FROM free_board
		<if test="fd!=null and fd!=''">
			WHERE subject LIKE '%'||#{fd}||'%'
		</if>
	</select>

	<insert id="freeBoardInsert" parameterType="com.sist.web.domain.community.vo.FreeBoardVO"
			useGeneratedKeys="true" keyProperty="no" keyColumn="NO">
		INSERT INTO FREE_BOARD (MEMBER_ID,SUBJECT,CONTENT,PWD,NAME)
		VALUES (#{member_id, jdbcType=INTEGER},#{subject},#{content},#{pwd, jdbcType=VARCHAR},#{name, jdbcType=VARCHAR})
	</insert>

	<update id="freeBoardUpdate" parameterType="com.sist.web.domain.community.vo.FreeBoardVO">
		UPDATE FREE_BOARD
		SET
		<if test="name!=null and name!=''">
			name=#{name},
		</if>
			subject=#{subject},content=#{content}
		WHERE no=#{no}
	</update>

	<delete id="freeBoardDelete" parameterType="int">
		DELETE FROM FREE_BOARD
		WHERE no=#{no}
	</delete>

	<select id="freeBoardPwdData" parameterType="int" resultType="String">
		SELECT pwd
		FROM FREE_BOARD
		WHERE no=#{no}
	</select>
	
</mapper>