<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.web.domain.exam.pgmapper.ExamPGMapper">

    <select id="findRecommendedCourses" resultType="com.sist.web.domain.exam.vo.RecommendCourseVO">
        SELECT
        course_no,
        COUNT(*) AS matched_count,
        AVG(similarity) AS avg_similarity,
        SUM(similarity) AS recommendation_score
        FROM course_question_map
        WHERE question_no IN
        <foreach collection="questionNos" item="questionNo"
                 open="("
                 separator=","
                 close=")">
            #{questionNo}
        </foreach>
        GROUP BY course_no
        ORDER BY recommendation_score DESC
        LIMIT 3
    </select>

</mapper>