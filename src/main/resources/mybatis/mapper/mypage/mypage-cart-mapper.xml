<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.web.domain.mypage.mapper.MyPageCartMapper">
	<select id="bookCartListData" resultType="com.sist.web.domain.book.vo.BookCartVO" parameterType="int">
		SELECT c.no,c.book_no,quantity,b.title,b.poster,b.publisher,b.price
		FROM book_cart c 
		JOIN book b ON b.no=c.book_no
		WHERE member_id=#{member_id}
		ORDER BY c.no DESC
		OFFSET #{start} ROWS FETCH NEXT 3 ROWS ONLY
	</select>
	<select id="bookCartRowCount" resultType="int" parameterType="int">
		SELECT COUNT(*) FROM book_cart WHERE member_id=#{member_id}
	</select>
</mapper>